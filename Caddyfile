{
    email c4j@faithconnect.us
    on_demand_tls {
        ask http://app:3000/api/caddy/ask
        interval 2m
        burst 5
    }
}

# The main application
build.faithconnect.us {
    reverse_proxy app:3000
}

# The dynamic preview subdomains
*.build.faithconnect.us {
    tls {
        on_demand
    }
    reverse_proxy app:3000
}
